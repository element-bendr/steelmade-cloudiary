import { ProductSeries } from '../../product-types';
import directorSeriesData from './director-series/index';

// Export the canonical director series data
export const directorSeries: ProductSeries = directorSeriesData;

// Optionally, re-export individual chairs if needed
// export * from './director-series/index';
