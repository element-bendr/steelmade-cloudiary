# Chair Factory and Component Templates

This document provides templates and examples for the standardized chair factory pattern and component structure to be used in the Director Series rebuild.

## Chair Interface

```typescript
// lib/types/chair.ts

export interface ChairVariant {
  id: string;
  name: string;
  imageUrl?: string; // Will be generated by the factory if not provided
  imageCode?: string; // Used to generate the imageUrl (e.g., "ic-123-hb")
}

export interface Chair {
  id: string;
  name: string;
  description: string;
  price: string; // Usually "Contact for pricing"
  imageUrl: string; // Primary image (usually first variant)
  category: string; // e.g., "director-series"
  variants: ChairVariant[];
  features: string[];
  defaultVariant: string; // ID of the default variant
}
```

## Chair Factory Function

```typescript
// lib/factories/chairFactory.ts

import { Chair, ChairVariant } from '@/lib/types/chair';

interface CreateDirectorChairParams {
  id: string;
  name: string;
  description: string;
  variants: Array<{
    id: string;
    name: string;
    imageCode: string;
  }>;
  features: string[];
  defaultVariant?: string;
}

/**
 * Generates a Cloudinary URL for a chair image
 */
export function getChairImageUrl(chairName: string, imageCode: string): string {
  return `https://res.cloudinary.com/dqde19mfs/image/upload/v1749458919/steelmade/chairs/director-series/${chairName}/${imageCode}.jpg`;
}

/**
 * Factory function to create a director chair with consistent structure
 */
export function createDirectorChair(params: CreateDirectorChairParams): Chair {
  const variants: ChairVariant[] = params.variants.map(variant => ({
    id: variant.id,
    name: variant.name,
    imageUrl: getChairImageUrl(params.id, variant.imageCode),
    imageCode: variant.imageCode
  }));

  const defaultVariant = params.defaultVariant || variants[0].id;
  const defaultVariantImage = variants.find(v => v.id === defaultVariant)?.imageUrl || variants[0].imageUrl;

  return {
    id: params.id,
    name: params.name,
    description: params.description,
    price: 'Contact for pricing',
    imageUrl: defaultVariantImage,
    category: 'director-series',
    variants,
    features: params.features,
    defaultVariant
  };
}
```

## Example Chair Data Implementation

```typescript
// lib/data/products/chairs/director-series/ashley.ts

import { createDirectorChair } from '@/lib/factories/chairFactory';

const ashleyDirectorChair = createDirectorChair({
  id: 'ashley',
  name: 'Ashley Director Chair',
  description: 'The Ashley Director Chair combines premium comfort with elegant design, perfect for professional settings.',
  variants: [
    {
      id: 'high-back',
      name: 'High Back',
      imageCode: 'ic-123-hb'
    },
    {
      id: 'mid-back',
      name: 'Mid Back',
      imageCode: 'ic-124-mb'
    }
  ],
  features: [
    'Premium aluminum frame',
    'Ergonomic design for extended comfort',
    'Weather-resistant materials',
    'Foldable for easy storage and transport',
    'Available in high-back and mid-back variants',
    'Durable construction for long-lasting use'
  ],
  defaultVariant: 'high-back'
});

export default ashleyDirectorChair;
```

## Chair Detail Page Template

```tsx
// app/chairs/director-series/[chairId]/page.tsx

"use client";

import React, { useState } from 'react';
import { notFound } from 'next/navigation';
import { ProductDetailLayout } from '@/components/products/ProductDetailLayout';
import { getDirectorChair } from '@/lib/data/products/chairs/director-series';

interface ChairDetailPageProps {
  params: {
    chairId: string;
  };
}

export default function ChairDetailPage({ params }: ChairDetailPageProps) {
  const chairId = params.chairId;
  const chair = getDirectorChair(chairId);
  
  if (!chair) {
    return notFound();
  }

  const [selectedVariant, setSelectedVariant] = useState(chair.defaultVariant);
  
  return (
    <ProductDetailLayout
      product={chair}
      selectedVariant={selectedVariant}
      onVariantChange={setSelectedVariant}
      breadcrumbs={[
        { name: 'Chairs', href: '/chairs' },
        { name: 'Director Series', href: '/chairs/director-series' },
        { name: chair.name, href: `/chairs/director-series/${chairId}` }
      ]}
    />
  );
}
```

## Director Series Index File Template

```typescript
// lib/data/products/chairs/director-series/index.ts

import ashleyDirectorChair from './ashley';
import operaDirectorChair from './opera';
import tycoonDirectorChair from './tycoon';
import bigbossGoldDirectorChair from './bigboss-gold';
import woodlandDirectorChair from './woodland';
import bostonDirectorChair from './boston';

export const directorSeriesChairs = [
  ashleyDirectorChair,
  operaDirectorChair,
  tycoonDirectorChair,
  bigbossGoldDirectorChair,
  woodlandDirectorChair,
  bostonDirectorChair
];

/**
 * Gets a specific director chair by ID
 */
export function getDirectorChair(chairId: string) {
  return directorSeriesChairs.find(chair => chair.id === chairId);
}

// Named exports for individual chairs
export { default as ashleyDirectorChair } from './ashley';
export { default as operaDirectorChair } from './opera';
export { default as tycoonDirectorChair } from './tycoon';
export { default as bigbossGoldDirectorChair } from './bigboss-gold';
export { default as woodlandDirectorChair } from './woodland';
export { default as bostonDirectorChair } from './boston';
```

## Chair Card Component Template

```tsx
// components/products/ChairCard.tsx

import React from 'react';
import Image from 'next/image';
import Link from 'next/link';
import { Chair } from '@/lib/types/chair';

interface ChairCardProps {
  chair: Chair;
  basePath?: string;
}

export function ChairCard({ chair, basePath = '/chairs/director-series' }: ChairCardProps) {
  return (
    <Link href={`${basePath}/${chair.id}`} className="group">
      <div className="bg-white rounded-lg overflow-hidden shadow-md transition-all duration-300 transform group-hover:scale-[1.02]">
        <div className="aspect-square relative overflow-hidden">
          <Image
            src={chair.imageUrl}
            alt={chair.name}
            fill
            sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"
            className="object-cover group-hover:scale-105 transition-transform duration-500"
          />
          <div className="absolute top-0 right-0 w-16 h-16 bg-red-700 opacity-20"></div>
        </div>
        <div className="p-4">
          <h3 className="text-lg font-semibold text-gray-900 group-hover:text-red-700 transition-colors">
            {chair.name}
          </h3>
          <p className="mt-1 text-sm text-gray-500">
            {chair.description.substring(0, 60)}
            {chair.description.length > 60 ? '...' : ''}
          </p>
        </div>
      </div>
    </Link>
  );
}
```

## Director Series Page Template

```tsx
// app/chairs/director-series/page.tsx

import React from 'react';
import { Metadata } from 'next';
import { ChairCard } from '@/components/products/ChairCard';
import { directorSeriesChairs } from '@/lib/data/products/chairs/director-series';

export const metadata: Metadata = {
  title: 'Director Series Chairs | SteelMade',
  description: 'Explore our premium Director Series chairs with elegant designs and superior comfort.',
};

export default async function DirectorSeriesPage() {
  return (
    <div className="container mx-auto py-12">
      <h1 className="text-3xl font-bold text-gray-900 mb-6">Director Series Chairs</h1>
      <p className="text-gray-600 mb-10 max-w-3xl">
        Discover our premium Director Series chairs, designed for professionals who demand the best in comfort and style.
        Each chair is crafted with attention to detail and built to last.
      </p>

      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {directorSeriesChairs.map((chair) => (
          <ChairCard key={chair.id} chair={chair} />
        ))}
      </div>
    </div>
  );
}
```

These templates provide a solid foundation for implementing the standardized chair system with consistent styling and structure across all director series products.